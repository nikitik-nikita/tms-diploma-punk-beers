import { createUseStyles } from 'react-jss';

const mainBodyFooterStyles = () => ({
  Root: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '15px 0',
  },
});

export default createUseStyles(mainBodyFooterStyles, { name: 'MainBodyFooter', index: 312 });
