import { createUseStyles } from 'react-jss';

const noFavouritesStyles = {
  Root: {
    color: '#00d1b2',
    fontSize: '150px',
    textAlign: 'center',
    fontFamily: 'cursive',
  },
};

export default createUseStyles(noFavouritesStyles, { name: 'noFavouritesStyles', index: 305 });
